# ==============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ==============================================================================
# 
# This file shows what environment variables are needed.
# 
# ⚠️  SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Replace ALL placeholder values with your own
# 3. NEVER commit .env.local to git (it's gitignored for security)
#
# Each person deploying this site will have their own unique values.
# The actual .env.local file is NEVER committed to git.
#
# ==============================================================================

# ==============================================================================
# REQUIRED SETTINGS
# ==============================================================================

# Google Gemini API Key
# Get one free at: https://aistudio.google.com/app/apikey
# This powers the AI chatbot. Without it, the chat won't work.
GEMINI_API_KEY=your_gemini_api_key_here

# Dashboard Password
# This is the password to access your admin dashboard at /dashboard
# Choose a strong password - this protects your ability to edit your CV
# Example: MyStr0ng!Pass#2024
DASHBOARD_PASSWORD=choose_a_strong_password_here

# JWT Secret Key
# Used to sign authentication tokens. Must be a long random string.
# Generate one with: openssl rand -base64 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=generate_a_random_string_here_at_least_32_chars

# ==============================================================================
# OPTIONAL SETTINGS
# ==============================================================================

# Node Environment
# Leave as production for deployed sites
NODE_ENV=production

# ==============================================================================
# DEPLOYMENT NOTES
# ==============================================================================
#
# VERCEL DEPLOYMENT:
# - Go to your project settings in Vercel
# - Navigate to Settings > Environment Variables
# - Add each variable from above
# - Variables are encrypted and secure in Vercel
#
# SELF-HOSTED:
# - Set these as system environment variables
# - Or use a .env.local file in your project root
#
# ==============================================================================
